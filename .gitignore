# Ignore everything
*

# ML training folder (still in training phase - not ready for production)
ml/

# But not the data folder and its contents
!data/
!data/**

# Don't ignore .gitignore itself
!.gitignore

# GitHub Actions workflow files
.github/
